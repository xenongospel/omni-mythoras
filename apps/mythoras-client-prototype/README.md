# Mythoras Client Prototype — throwaway (Three.js)

One battle screen: a 3D voxel tile board over a traced Darkest Dungeon 1 combat plate.

```bash
cd apps/mythoras-client-prototype
bun install
bun run dev
```

Spec: `POSTURE_REQUIREMENTS.md`. Prompt: `SYSTEM_PROMPT.md`.
