.DS_Store
.scratch/
*.mp4
*.mov
node_modules/
dist/
.env
.env.*

# Local MCP server config. Holds API keys in plain text — this repo is public.
.cursor/mcp.json
apps/*/node_modules/
apps/*/dist/

# Third-party game screenshots and extracted art used as local agent
# reference only. Kept on disk, never published — this repo is public.
# READMEs stay tracked so the expected structure is documented.
apps/*/reference/**/*.jpg
apps/*/reference/**/*.jpeg
apps/*/reference/**/*.png
apps/*/reference/**/*.webp
apps/*/reference/**/*.gif
