# AGENTS.md

Pointers for agent skills operating in this Mythoras planning / monorepo harness workspace.

## Agent skills

### Issue tracker

GitHub Issues on `xenongospel/omni-mythoras`; external PRs are also a triage request surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context (monorepo star): root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
